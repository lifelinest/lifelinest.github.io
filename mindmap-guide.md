# 安知鱼主题思维导图功能集成指南

## 功能概述

本指南介绍了如何在Hexo安知鱼主题中集成思维导图功能，支持两种使用方式：

1. **自动生成**：通过文章标题结构自动生成思维导图
2. **直接插入**：使用 `{% mindmap %}` 标签在Markdown中直接插入思维导图

## 技术栈

- **核心库**：Markmap.js（基于D3.js）
- **集成方式**：Hexo自定义标签插件 + CDN依赖
- **主题适配**：安知鱼主题（最新版）

## 已完成的修改

### 1. 自定义标签插件
- **文件**：`scripts/mindmap.js`
- **功能**：支持 `{% mindmap %}` 标签语法

### 2. 脚本增强
- **文件**：`themes/anzhiyu/source/js/mindmap.js`
- **功能**：添加内联思维导图渲染支持

### 3. 样式适配
- **文件**：`themes/anzhiyu/source/css/_tags/mindmap.styl`
- **功能**：添加内联思维导图样式，适配暗黑模式和移动端

### 4. 模板修改
- **文件**：`themes/anzhiyu/layout/post.pug`
- **功能**：添加思维导图模态框和按钮

### 5. 配置选项
- **文件**：`themes/anzhiyu/_config.anzhiyu.yml`
- **功能**：添加思维导图功能开关和配置

## 测试步骤

### 本地测试

1. **启动本地服务**
   ```bash
   hexo clean && hexo s
   ```

2. **访问测试文章**
   ```
   http://localhost:4000/2024/01/04/mindmap-test/
   ```

3. **测试功能**
   - 点击右下角思维导图按钮，查看自动生成的思维导图
   - 测试节点折叠/展开功能
   - 测试全屏切换功能
   - 测试导出功能
   - 查看直接插入的思维导图渲染效果
   - 切换暗黑模式，查看样式适配
   - 在移动端查看响应式效果

### 功能验证清单

| 功能项 | 预期结果 | 验证方法 |
|--------|----------|----------|
| 自动生成思维导图 | 根据文章标题结构生成完整思维导图 | 点击右下角按钮查看 |
| 内联思维导图渲染 | Markdown中的标签正确渲染为思维导图 | 直接查看文章内容 |
| 节点交互 | 支持节点折叠/展开 | 点击节点测试 |
| 拖拽平移 | 支持思维导图拖拽平移 | 鼠标拖拽测试 |
| 缩放功能 | 支持思维导图缩放 | 鼠标滚轮测试 |
| 全屏查看 | 支持全屏切换 | 点击全屏按钮测试 |
| 导出SVG | 支持导出为SVG文件 | 点击导出按钮测试 |
| 暗黑模式适配 | 样式适配暗黑模式 | 切换主题模式查看 |
| 移动端响应式 | 在移动端正常显示 | 使用手机访问或浏览器模拟 |

## 优化建议

### 1. 性能优化

- **按需加载**：目前已实现per_page配置，可根据需要调整
- **CDN优化**：使用稳定的CDN源，可根据地区选择更快的CDN
- **缓存机制**：思维导图渲染结果可考虑添加缓存，减少重复渲染

### 2. 样式优化

- **自定义主题色**：可根据喜好调整思维导图节点颜色
- **字体优化**：调整节点字体大小和行高，提升可读性
- **间距调整**：根据文章布局调整思维导图容器间距

### 3. 功能扩展

- **支持更多导出格式**：如PNG、PDF等
- **支持主题切换**：提供多种思维导图主题样式
- **支持节点链接**：允许在节点中添加链接

## 部署步骤

### 本地构建

```bash
hexo clean && hexo g
```

### 部署到服务器

根据您的部署方式选择：

#### 方式1：Git部署
```bash
hexo d
```

#### 方式2：手动部署
```bash
# 将public目录内容上传到服务器
rsync -avz public/ user@server:/path/to/website
```

### 验证部署

1. 访问部署后的网站
2. 查看测试文章，验证思维导图功能正常
3. 检查浏览器控制台，确保没有错误

## 可能的坑及解决方案

### 1. 样式冲突

**问题**：思维导图样式与主题其他样式冲突

**解决方案**：
- 使用更具体的CSS选择器
- 添加样式隔离，如使用CSS Modules
- 调整样式加载顺序

### 2. 渲染延迟

**问题**：思维导图渲染较慢，影响页面加载速度

**解决方案**：
- 优化依赖加载顺序
- 使用懒加载，延迟渲染非首屏思维导图
- 考虑使用预渲染或服务端渲染

### 3. 移动端显示问题

**问题**：在移动端显示异常，如节点重叠、无法交互

**解决方案**：
- 调整移动端样式，增加节点间距
- 优化触摸交互，支持移动端手势
- 考虑在小屏幕设备上默认展开较少层级

### 4. 依赖加载失败

**问题**：CDN依赖加载失败，导致思维导图无法渲染

**解决方案**：
- 添加依赖加载失败的 fallback 机制
- 考虑本地托管依赖文件
- 使用多个CDN源，增加可靠性

### 5. 与其他插件冲突

**问题**：与其他Hexo插件或主题功能冲突

**解决方案**：
- 检查插件之间的兼容性
- 调整插件加载顺序
- 禁用冲突的插件或功能

## 使用示例

### 1. 自动生成思维导图

在文章Front Matter中添加：

```yaml
title: 文章标题
date: 2024-01-04 14:00:00
mindmap: true
```

### 2. 直接插入思维导图

在Markdown中使用：

```markdown
{% mindmap %}
# 中心主题
## 分支1
### 子分支1
### 子分支2
## 分支2
### 子分支3
{% endmindmap %}
```

## 配置说明

在 `themes/anzhiyu/_config.anzhiyu.yml` 中配置：

```yaml
# 思维导图功能
mindmap:
  enable: true # 是否启用
  per_page: true # 是否在单页文章中加载
  position: bottom-right # 按钮位置
  style: default # 按钮样式
  auto_expand: false # 是否自动展开所有节点
  export:
    enable: true
    formats: ['svg']
  fullscreen:
    enable: true
    button_text: '全屏查看'
  mobile:
    enable: true
    only: false
```

## 更新日志

### v1.0.0
- 初始版本
- 支持自动生成思维导图
- 支持直接插入思维导图
- 适配暗黑模式和移动端
- 支持全屏和导出功能

## 注意事项

1. 思维导图节点层级建议不超过5层
2. 每个节点文本不宜过长（建议≤20字符）
3. 确保文章标题结构规范，使用正确的Markdown标题语法
4. 定期更新依赖库，确保功能正常
5. 在生产环境前进行充分测试

## 参考资源

- [Markmap.js官方文档](https://markmap.js.org/)
- [D3.js官方文档](https://d3js.org/)
- [Hexo官方文档](https://hexo.io/zh-cn/docs/)
- [安知鱼主题文档](https://anzhiy.cn/)  